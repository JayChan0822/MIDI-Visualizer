-- V1.1 --

- 修复某些浏览器调色盘闪退问题
- 加入Z-Space滑块

-- V1.2 --

- 加入“视角参数”设置功能，保证多工程视角的统一（可导出导入参数）
- 加入播放区域高光颜色更改功能（推荐亮色）
- 加入上下区域调整把手
- 支持MIDI、音频、图片、视频、视角JSON、工程JSON拖动导入
- 可视化区域“Command/Control+双击”可重置视角，按住“Command/Control”拖动可平移，滚轮放大缩小
- “导入文件”的操作支持撤回
- 优化midi导入轨道的同步
- 删除了音符贴图和timeline zoom的调节

-- V1.2.1 --
- 修复导入/导出工程